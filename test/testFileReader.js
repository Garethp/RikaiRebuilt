const FileReader = require('../src/FileReader');

const assert = require('assert');

describe('FileReader', async () => {
    describe('read()', async () => {
        it('should read a file', async () => {
            // const test = await FileReader.read('./test.txt');

            // assert.equal('Hello', test);
        });
    });
});